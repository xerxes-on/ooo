<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================ -->
    <!-- SECURITY GROUPS (Optional - uncomment if needed)             -->
    <!-- ============================================================ -->

    <!--
    <record id="module_category_custom_odoo" model="ir.module.category">
        <field name="name">Custom Odoo</field>
        <field name="description">Custom Odoo Module Category</field>
        <field name="sequence">100</field>
    </record>

    <record id="group_custom_odoo_user" model="res.groups">
        <field name="name">User</field>
        <field name="category_id" ref="module_category_custom_odoo"/>
        <field name="implied_ids" eval="[(4, ref('base.group_user'))]"/>
    </record>

    <record id="group_custom_odoo_manager" model="res.groups">
        <field name="name">Manager</field>
        <field name="category_id" ref="module_category_custom_odoo"/>
        <field name="implied_ids" eval="[(4, ref('group_custom_odoo_user'))]"/>
        <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
    </record>
    -->

    <!-- ============================================================ -->
    <!-- RECORD RULES (Optional - uncomment if needed)                -->
    <!-- ============================================================ -->

    <!--
    <record id="x_custom_sample_rule_user" model="ir.rule">
        <field name="name">Sample: User can see own records</field>
        <field name="model_id" ref="model_x_custom_sample"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('group_custom_odoo_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <record id="x_custom_sample_rule_manager" model="ir.rule">
        <field name="name">Sample: Manager can see all records</field>
        <field name="model_id" ref="model_x_custom_sample"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_custom_odoo_manager'))]"/>
    </record>

    <record id="x_custom_sample_rule_company" model="ir.rule">
        <field name="name">Sample: Multi-company rule</field>
        <field name="model_id" ref="model_x_custom_sample"/>
        <field name="domain_force">[('company_id', 'in', company_ids)]</field>
        <field name="global" eval="True"/>
    </record>
    -->

</odoo>
